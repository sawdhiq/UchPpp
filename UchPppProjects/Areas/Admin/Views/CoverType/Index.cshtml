@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}



<div class="container p-3">
    <div class=row pt-2>
        <div class="col-6">
            <h2 class="text-primary"></h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-circle"></i> &nbsp; Add New CoverType </a>
            </div>
        </div>

    



<h1 style="text-align:center; font-size: large; padding-bottom: 5px;">COVERTYPES NAMES</h1>

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr style="text-align:left;">
           
            <th style="width:80%;">
                NAMES
            </th>
            <th>
                ACTIONS
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                 <td width="30%">
                    @obj.Name
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a class="btn btn-primary" style="width: 150px;" asp-controller="CoverType" asp-action="Edit" asp-route-id="@obj.Id">
                            <i class="bi bi-pencil-square"></i>&nbsp;Edit</a>
                            &nbsp;&nbsp;&nbsp;&nbsp; <a class="btn btn-danger" style="width: 170px;" asp-controller="CoverType" asp-action="Delete" asp-route-id="@obj.Id">
                            <i class="bi bi-trash-fill"></i>&nbsp; Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>